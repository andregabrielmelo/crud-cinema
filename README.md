# Como usar

## Inicializar projeto

cd my-app

(yarn | npm) run dev

## Inicializar base de dados

docker compose up -d

npx prisma db push

## Conectar com base de dados

mysql --host=127.0.0.1 --port=3310 -u root -p
